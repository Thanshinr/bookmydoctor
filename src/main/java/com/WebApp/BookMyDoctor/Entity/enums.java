package com.WebApp.BookMyDoctor.Entity;

public class enums {
         public enum Role {
                ADMIN, DOCTOR, PATIENT
        }

       public enum Status {
                APPROVED, REJECTED,PENDING
        }
}